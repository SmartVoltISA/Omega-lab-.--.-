# Ω-Lab — WORK ARCHIVE INDEX v1.0

**Status:** CORE / ACTIVE  
**Date:** 2026-08-18

## Current cycle

### SPACE

Primary active work in the current cycle:

1. Ω-PRESENT-1 — current state separated from future intent; append-only state transition and MEMORY↔PRESENT ledger.
2. Ω-PLAN-1 — future orientation converted into an auditable proposed plan with expected results.
3. Ω-EXECUTION-1 — bounded execution behind Guardian authorization.
4. Ω-VERIFICATION-1 — explicit expected-vs-actual verification boundary.
5. Ω-FEEDBACK-1 — result converted into structured feedback without silently mutating PRESENT.
6. Ω-CYCLE-1 — intended closed operational cycle from plan through execution, result, verification and feedback.

These items are SPACE work because their primary subject is the SPACE organism and its operational organs.

### OMEGA

The governing architectural principles remain in OMEGA, including:

- memory as structural persistence;
- bidirectional whole↔parts transformation;
- relations-first graph architecture;
- history preservation;
- CI execution gating;
- application pipeline;
- provenance and project-boundary rules.

### MARKET

MARKET retains its own market-state, data, experiments, hypotheses, replay/walk-forward results, application decisions and implementation history. SPACE architecture must not overwrite or absorb MARKET-specific evidence.

## Archival principle

This index is a navigation layer, not a replacement for the underlying records. Detailed history remains in the original experiment/project paths.

## Verification rule

No item is marked VERIFIED merely because code exists. The Ω-Lab CI gate requires an actual CI execution and recorded result.

## Cross-project reuse

When an artifact is demonstrated to be genuinely shared, create explicit references in each applicable project rather than silently relocating the artifact.

```text
                 OMEGA
              /    |    \
             /     |     \
          SPACE  SHARED  MARKET
```

`SHARED` means proven or explicitly intended common architecture; it does not erase project-specific state.

## Recovery rule

If a future conversation needs to reconstruct this work, start from this index, then follow the experiment IDs and commit history. Never reconstruct by guessing when an archival source exists.
